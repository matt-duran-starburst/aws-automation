# Core CLI dependencies
click>=8.0.0
boto3>=1.26.0
botocore>=1.29.0
PyYAML>=6.0
python-dotenv>=0.19.0

# Infrastructure Management
pulumi>=3.0.0
pulumi-aws>=6.0.0
pulumi-gcp>=7.0.0
pulumi-azure>=5.0.0

# Kubernetes and Local Cluster Management
kubernetes>=25.0.0
#kind>=0.1.0  # Python wrapper for Kind
docker>=6.0.0

# Helm Integration (using subprocess to call Helm CLI directly)
# No Python package needed - Helm CLI must be installed separately

# SSH and Connectivity
paramiko>=3.0.0  # SSH client library
sshtunnel>=0.4.0  # SSH tunnel management
pexpect>=4.8.0  # For interactive SSH operations

# Template Generation
jinja2>=3.1.0  # For Helm values template generation
jsonschema>=4.0.0  # Configuration validation

# Development dependencies (uncomment for development)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# flake8>=5.0.0
# mypy>=0.991

# Testing for new components
# pytest-kubernetes>=1.0.0
# pytest-docker>=1.0.0